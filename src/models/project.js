class Project {
    constructor(name, url, plugin, platform, description){
        this.name = name;
        this.url = url;
        this.plugin = plugin;
        this.platform = platform;
        this.description = description;
    }
}

export default Project